# 디자인 연결 검증 결과 — 2026-09-12

## 실제 확인
- 운영 Apps Script 웹 앱: 신청수집, 버전 1 (2026-08-29). 기존 페이지 주소와 배포 관리 화면 주소 일치.
- 17:59:33 KST: 새 화면 → 시험 서버 → 기존 웹 앱으로 강좌 한 건 전송.
- 운영 신청자 시트 33행: 지정 번호 일치, 강좌 gsei-1468, 접수 2026-09-23 09:00 확인.
- 시험 완료 후 해당 행 C33의 번호를 비우고 B33에 시험 완료 표시. 기존 다른 신청 행은 수정하지 않음.
- 별도 self-test 예약: 가상 접수 18:25 KST, 문자 예약 18:10 KST.
- 15분전예약 시트 21행: 상태 발송완료, 업체발송시각 2026-09-12T09:10:00.907Z.
- 사용자 수신 확인 및 휴대폰 사진의 오후 6:10 표시 확인.
- CP15_TEST_PHONE와 CP15_TEST_OPEN_AT을 시험 전 값으로 복원. 해당 시험에서는 자동 발송 활성화 설정을 변경하지 않음. 이후 본문 수정은 아래 최종 갱신 참조.
- 실제 연결 시험 서버(4175)는 종료. 4174는 모의 시험 전용으로 유지.
- 시트 저장 시험과 시험 예약 발송은 각각 검증했다. 동일한 실강좌의 접수일까지 기다린 종단 시험은 아니다.

## 코드 검증
- 자동 테스트 58개 통과: 기존 전송 필드, 부분 성공 재시도, 응답 유실, 저장소 오류, 시간 변경, 기존 기록 승계, 수집 JSON 블록, 예약/취소/중복 처리.
- 기존 신청 형식과 웹 앱을 유지. 배포 검토본은 노트북의 Node 서버 없이 GitHub Pages에서 작동하도록 구성.
- 기존 ontime.likes / ontime.submitted / ontime.signups / ontime.queue를 보존. 새 상태 기록에 기존 신청 이력을 승계.
- 기존 큐에 미확인 신청이 있으면 자동 재전송하지 않고 안내. 운영 정책의 의도적 변경으로, 재시도 중복 방지 목적.
- no-cors 응답은 저장 성공을 증명하지 않는다. 운영 UI는 '신청 전송'으로 안내하고 중복 재전송을 억제한다.
- 접수 15분 전 이후 신청 차단. 일정 종료/접수 마감 숨김. 네트워크 오류 시에도 시간 경과에 따른 상태를 재평가.
- 강좌 데이터 블록 programsData / dataUpdatedAt / sourceUpdatedAt을 기존과 동일하게 유지.
- 오전 프리셋 제목, 누구나 참여 가능한 프로그램의 대상 분류 보완.
- 선호 시간대 알림의 신규 신청 UI는 이번 범위에서 제외. 기존 신청자의 서버 설정은 유지.

## 배포 검토본
- 생성: node culturepick-integration/build-candidate.cjs
- 검토: http://127.0.0.1:4174/candidate/
- 모의 테스트: http://127.0.0.1:4174/
- candidate는 생성물이다. 배포 직전 새로 생성하여 최신 기관 데이터를 포함해야 한다.
- 로컬 운영 자료와 문자 코드가 변경되었으며 복원 태그는 생성했다. 새 화면은 미배포 상태다.

## 남은 한계와 배포 순서
- 실제 iPhone/Android에서 숫자 키패드가 뜬 상태의 화면 배치는 별도 확인이 필요하다. 문자 수신 사진은 입력 화면 시험이 아니다.
- GA 이벤트 코드는 연결했으나 실서비스 GA 보고서 유입은 배포 후 확인한다. 로컬에서는 GA와 외부 신청을 차단한다.
- 히어로 PNG 두 장은 합계 약 4.4MB다. 느린 모바일 회선의 로딩은 추가 최적화 여지가 있다.
- 문자 본문의 '그만 회신'은 자동 수신거부 처리 검증이 아니다. 이후 본문에서 회신 안내를 제거했다. 아래 최종 갱신 참조.
- 포항시립도서관의 로컬 수집 의존성은 기존과 같다. 디자인 교체로 해결되는 항목이 아니다.
- 실제 Pages 배포 커밋과 복원 태그를 확보했다. 최종 승인 후 배포한다.
- 화면 교체는 독립 커밋으로 배포하고 신청/링크/자료 최신성을 재확인한다.
- 복원은 화면 변경을 되돌리는 새 커밋으로 수행하며 최신 기관 데이터를 보존한다. 시트와 문자 기록은 화면 복원과 별개다.


## 최종 갱신 — 2026-09-12
- 자동 검증 58개 전체 통과. 실제 시트 저장과 별도 예약 문자 수신 확인 완료.
- 휴대폰 실기기 입력 검증은 사용자 요청으로 보류. 모의 모바일 화면 입력·동의·신청 확인, LAN 시험 서버 4176 종료.
- 실제 Apps Script 두 문자 파일과 저장소 복사본에서 그만 회신 안내를 제거하여 저장. 새로 생성되는 본문에 적용되며 이미 예약된 본문은 수정하지 않음. 후보 화면에서 이메일로 알림 중지·개인정보 삭제 요청 안내.
- 19:12 KST 시작한 수집 정상 완료: 전체816건, 실패 기관 없음. 화면 자료18건→16건. phlib-3551, phlib-3576 제외. 최신 자료로 후보 재생성 완료.
- 실제 Pages 배포 커밋 19cd08d3974804fe3fb882866d1dcd0e11af5442 확인. 복원 태그 before-design-20260912-19cd08d 원격 보관 완료, 동일 이름 ZIP 로컬 보관.
- 로컬 운영 파일은 데이터와 문자 본문이 변경됨. 새 화면은 candidate에만 준비되어 있고 Pages에는 아직 배포하지 않음.
- 복원 시 새 커밋으로 화면을 되돌리고 최신 programs.json 및 index.html의 데이터 블록은 보존. Apps Script·시트·문자 기록은 화면 복원과 별개.
- 실제 배포는 최종 승인 후 진행. GA 실유입 확인은 배포 후 수행.


## 배포 완료
- 사용자 승인 후 ddc1bee078552a7fa8abe37d3ac03edfb8cbce4c를 기존 GitHub Pages에 배포.
- 실행 결과 success: https://github.com/kukheehan98-lgtm/p_landing_p/actions/runs/34688381060
- 실제 주소의11개 파일 HTTP200 및 배포본 일치 확인(텍스트 LF 정규화).
- 실제 화면16개 프로그램, 담기0→1→0, 신청창·동의 안내 정상, 브라우저 경고·오류0건.
- 확인 중 번호 입력·신청 제출·문자 추가 발송 없음. 실기기 숫자 키패드는 계속 보류.
- GA 설정과 유입 태그 코드는 유지. GA 보고서 집계는 별도 확인 대상.
